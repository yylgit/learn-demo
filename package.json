{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna publish --no-git-tag-version --no-push",
    "version": "lerna version --no-git-tag-version --no-push"
  },
  "dependencies": {
    "lerna": "^3.10.7"
  }
}
